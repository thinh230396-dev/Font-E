HOST=0.0.0.0
PORT=3000

# Bật cửa đăng nhập nhanh của dev server (mặc định tắt).
# Khi bằng 1, GET /api/auth/dev-login?role=SUPERADMIN cấp phiên không cần mật khẩu.
# Chỉ có tác dụng trong `npm run dev`; xác thực khi deploy không dùng plugin này.
# SALONSYS_DEV_LOGIN=1
