import { FormEvent, useEffect, useMemo, useState } from 'react';
import {
  ArrowUpRight,
  BarChart3,
  Check,
  ChevronRight,
  CircleDollarSign,
  Clock3,
  Download,
  Eye,
  EyeOff,
  Filter,
  Image,
  Layers3,
  LayoutGrid,
  LayoutList,
  PackageCheck,
  Palette,
  Pencil,
  Plus,
  Search,
  Sparkles,
  Star,
  Store,
  Tag,
  TrendingUp,
  UserRoundCheck,
  X
} from 'lucide-react';
import BeautifulSelect from './BeautifulSelect';

type BranchCode = 'Q1' | 'Q3';
type LibraryTab = 'DESIGNS' | 'COLORS';
type ViewMode = 'GRID' | 'LIST';
type DesignStatus = 'TRENDING' | 'ACTIVE' | 'DRAFT' | 'HIDDEN';
type ColorStatus = 'ACTIVE' | 'LOW' | 'OUT';

interface NailDesign {
  id: string;
  name: string;
  collection: string;
  styles: string[];
  level: 1 | 2 | 3 | 4;
  duration: number;
  surcharge: number;
  status: DesignStatus;
  branches: BranchCode[];
  colors: Array<{ name: string; hex: string; code: string }>;
  materials: string[];
  technicians: string[];
  bookings: number;
  rating: number;
  revenue: number;
  online: boolean;
  updatedAt: string;
  notes: string;
  preview: number;
}

interface PolishColor {
  id: string;
  name: string;
  brand: string;
  code: string;
  hex: string;
  finish: string;
  stock: number;
  minimumStock: number;
  monthlyUsage: number;
  linkedDesigns: number;
  status: ColorStatus;
  branches: BranchCode[];
  collection: string;
  location: string;
  cost: number;
  updatedAt: string;
}

interface TenantAdminNailGalleryProps {
  searchQuery: string;
  onSearchQueryChange: (value: string) => void;
  selectedBranch: string;
  onSelectedBranchChange: (value: string) => void;
  tenantName?: string;
  roleLabel?: string;
  accessMode?: 'full' | 'limited' | 'locked';
  readOnlyReason?: string;
  onNotify?: (message: string) => void;
}

const designSeed: NailDesign[] = [
  { id: 'NAIL-184', name: 'Crystal French Chrome', collection: 'Summer Light 2026', styles: ['French', 'Chrome', 'Đính đá'], level: 3, duration: 75, surcharge: 680000, status: 'TRENDING', branches: ['Q1', 'Q3'], colors: [{ name: 'Bubble Bath', hex: '#e9c9c2', code: 'OPI-S86' }, { name: 'Aurora Pearl', hex: '#d9d4ea', code: 'AP-03' }, { name: 'Silver Veil', hex: '#b9bec8', code: 'SV-12' }], materials: ['Crystal Mix SS3', 'Chrome Aurora', 'Top Coat No Wipe'], technicians: ['Thảo Nguyễn', 'Hà My'], bookings: 68, rating: 4.9, revenue: 46240000, online: true, updatedAt: '18/07/2026 · 14:32', notes: 'Áp dụng giá phụ thu cho bộ 10 móng. Cần xác nhận độ dài móng trước khi đặt lịch.', preview: 0 },
  { id: 'NAIL-176', name: 'Milky Ombre Blossom', collection: 'Spring Minimal', styles: ['Ombre', 'Vẽ hoa', 'Minimal'], level: 2, duration: 45, surcharge: 420000, status: 'ACTIVE', branches: ['Q1', 'Q3'], colors: [{ name: 'Milky White', hex: '#f5eee8', code: 'DND-MW01' }, { name: 'Blush Petal', hex: '#efb7c0', code: 'BP-08' }, { name: 'Sage Leaf', hex: '#aebca3', code: 'SL-04' }], materials: ['Art Gel Pastel 02', 'Cọ nét 7mm', 'Matte Top'], technicians: ['Minh Châu', 'Hà My', 'Thuỳ Dương', 'Bảo Ngọc', 'Thảo Nguyễn'], bookings: 46, rating: 4.8, revenue: 19320000, online: true, updatedAt: '17/07/2026 · 09:18', notes: 'Mẫu phù hợp móng ngắn đến trung bình. Có thể đổi tông hoa theo yêu cầu khách.', preview: 1 },
  { id: 'NAIL-169', name: 'Red Wine Velvet', collection: 'Evening Glam', styles: ['Velvet', 'Cat Eye'], level: 2, duration: 30, surcharge: 350000, status: 'ACTIVE', branches: ['Q1', 'Q3'], colors: [{ name: 'Merlot', hex: '#681c2c', code: 'DND-751' }, { name: 'Ruby Beam', hex: '#9f253f', code: 'RB-05' }, { name: 'Gold Line', hex: '#cba45d', code: 'GL-02' }], materials: ['Cat Eye Magnet 05', 'Gold Line Gel', 'Glossy Top'], technicians: ['Minh Châu', 'Thảo Nguyễn', 'Hà My', 'Bảo Ngọc', 'Thuỳ Dương', 'Kim Anh'], bookings: 52, rating: 4.9, revenue: 18200000, online: true, updatedAt: '16/07/2026 · 18:05', notes: 'Hiệu ứng đẹp nhất trên form almond. Chụp ảnh dưới ánh sáng nghiêng để thấy rõ vệt mắt mèo.', preview: 2 },
  { id: 'NAIL-192', name: 'Ocean Glass 3D', collection: 'August Preview', styles: ['3D Gel', 'Glass', 'Ocean'], level: 4, duration: 120, surcharge: 1100000, status: 'DRAFT', branches: ['Q3'], colors: [{ name: 'Glass Blue', hex: '#7bc5d9', code: 'GB-02' }, { name: 'Deep Ocean', hex: '#135a78', code: 'DO-09' }, { name: 'Shell Pearl', hex: '#eadfd0', code: 'SP-11' }], materials: ['3D Clear Gel', 'Shell Flake', 'Silver Foil', 'Builder Gel'], technicians: ['Thảo Nguyễn'], bookings: 0, rating: 0, revenue: 0, online: false, updatedAt: '19/07/2026 · 10:12', notes: 'Đang chờ duyệt ảnh và hoàn tất đào tạo kỹ thuật viên. Dự kiến mở bán 01/08/2026.', preview: 3 },
  { id: 'NAIL-158', name: 'Soft Aura Nude', collection: 'Everyday Signature', styles: ['Aura', 'Nude', 'Minimal'], level: 2, duration: 40, surcharge: 320000, status: 'ACTIVE', branches: ['Q1'], colors: [{ name: 'Warm Nude', hex: '#d7aa93', code: 'WN-14' }, { name: 'Soft Peach', hex: '#eac0aa', code: 'SP-07' }, { name: 'Milk Veil', hex: '#f1e5da', code: 'MV-01' }], materials: ['Airbrush White', 'Blooming Gel', 'Glossy Top'], technicians: ['Minh Châu', 'Thuỳ Dương', 'Kim Anh'], bookings: 39, rating: 4.7, revenue: 12480000, online: true, updatedAt: '14/07/2026 · 16:40', notes: 'Mẫu bán chạy cho khách văn phòng. Cho phép chọn nền nude theo tông da.', preview: 4 },
  { id: 'NAIL-147', name: 'Black Gold Geometry', collection: 'Editorial 2026', styles: ['Geometric', 'Metallic'], level: 3, duration: 65, surcharge: 590000, status: 'HIDDEN', branches: ['Q3'], colors: [{ name: 'Onyx', hex: '#171719', code: 'OX-01' }, { name: 'Liquid Gold', hex: '#c99b42', code: 'LG-08' }], materials: ['Foil Gold', 'Line Art Gel', 'Matte Top'], technicians: ['Thảo Nguyễn', 'Hà My'], bookings: 21, rating: 4.8, revenue: 12390000, online: false, updatedAt: '10/07/2026 · 11:28', notes: 'Tạm ẩn do foil gold đúng tiêu chuẩn đang chờ nhập kho.', preview: 5 }
];

const colorSeed: PolishColor[] = [
  { id: 'CLR-OPI-S86', name: 'Bubble Bath', brand: 'OPI', code: 'NL S86', hex: '#e9c9c2', finish: 'Sheer · Nude', stock: 5, minimumStock: 12, monthlyUsage: 86, linkedDesigns: 42, status: 'LOW', branches: ['Q1', 'Q3'], collection: 'Classic', location: 'Kệ G-01', cost: 390000, updatedAt: '18/07/2026' },
  { id: 'CLR-DND-751', name: 'Merlot', brand: 'DND', code: '751', hex: '#681c2c', finish: 'Cream · Opaque', stock: 3, minimumStock: 12, monthlyUsage: 64, linkedDesigns: 28, status: 'LOW', branches: ['Q1', 'Q3'], collection: 'Diva Duo', location: 'Kệ G-02', cost: 275000, updatedAt: '18/07/2026' },
  { id: 'CLR-AP-03', name: 'Aurora Pearl', brand: 'NailPro', code: 'AP-03', hex: '#d9d4ea', finish: 'Chrome · Pearl', stock: 8, minimumStock: 5, monthlyUsage: 42, linkedDesigns: 31, status: 'ACTIVE', branches: ['Q1', 'Q3'], collection: 'Aurora', location: 'Kệ C-04', cost: 460000, updatedAt: '17/07/2026' },
  { id: 'CLR-GB-02', name: 'Glass Blue', brand: 'Jello Jello', code: 'GB-02', hex: '#7bc5d9', finish: 'Jelly · Translucent', stock: 7, minimumStock: 6, monthlyUsage: 18, linkedDesigns: 12, status: 'ACTIVE', branches: ['Q3'], collection: 'Ocean Glass', location: 'Kệ G-06', cost: 420000, updatedAt: '16/07/2026' },
  { id: 'CLR-MW-01', name: 'Milky White', brand: 'DND', code: 'MW-01', hex: '#f5eee8', finish: 'Milky · Semi sheer', stock: 0, minimumStock: 10, monthlyUsage: 72, linkedDesigns: 36, status: 'OUT', branches: ['Q1', 'Q3'], collection: 'Milky Base', location: 'Kệ G-01', cost: 295000, updatedAt: '19/07/2026' },
  { id: 'CLR-OX-01', name: 'Onyx Black', brand: 'The GelBottle', code: 'OX-01', hex: '#171719', finish: 'Cream · Opaque', stock: 11, minimumStock: 6, monthlyUsage: 26, linkedDesigns: 19, status: 'ACTIVE', branches: ['Q3'], collection: 'Core Colors', location: 'Kệ G-03', cost: 510000, updatedAt: '12/07/2026' },
  { id: 'CLR-SG-04', name: 'Sage Leaf', brand: 'Kokoist', code: 'SG-04', hex: '#aebca3', finish: 'Cream · Muted', stock: 9, minimumStock: 6, monthlyUsage: 31, linkedDesigns: 17, status: 'ACTIVE', branches: ['Q1'], collection: 'Nature Edit', location: 'Kệ G-05', cost: 485000, updatedAt: '15/07/2026' },
  { id: 'CLR-LG-08', name: 'Liquid Gold', brand: 'Presto', code: 'LG-08', hex: '#c99b42', finish: 'Metallic · Foil', stock: 2, minimumStock: 5, monthlyUsage: 22, linkedDesigns: 14, status: 'LOW', branches: ['Q3'], collection: 'Metallic Art', location: 'Kệ C-03', cost: 560000, updatedAt: '19/07/2026' }
];

const designStatusMeta: Record<DesignStatus, { label: string; badge: string }> = {
  TRENDING: { label: 'Đang thịnh hành', badge: 'bg-violet-50 text-violet-700 ring-violet-200' },
  ACTIVE: { label: 'Đang mở bán', badge: 'bg-emerald-50 text-emerald-700 ring-emerald-200' },
  DRAFT: { label: 'Bản nháp', badge: 'bg-amber-50 text-amber-700 ring-amber-200' },
  HIDDEN: { label: 'Đang ẩn', badge: 'bg-slate-100 text-slate-600 ring-slate-200' }
};

const colorStatusMeta: Record<ColorStatus, { label: string; badge: string; dot: string }> = {
  ACTIVE: { label: 'Sẵn sàng', badge: 'bg-emerald-50 text-emerald-700 ring-emerald-200', dot: 'bg-emerald-500' },
  LOW: { label: 'Sắp hết', badge: 'bg-amber-50 text-amber-700 ring-amber-200', dot: 'bg-amber-500' },
  OUT: { label: 'Hết hàng', badge: 'bg-rose-50 text-rose-700 ring-rose-200', dot: 'bg-rose-500' }
};

const previewBackgrounds = [
  ['#f3e5e2', '#d9d4ea', '#ffffff'],
  ['#f5eee8', '#efb7c0', '#aebca3'],
  ['#681c2c', '#9f253f', '#cba45d'],
  ['#7bc5d9', '#135a78', '#eadfd0'],
  ['#d7aa93', '#eac0aa', '#f1e5da'],
  ['#171719', '#c99b42', '#f5f0e6']
];

const inputClass = 'h-11 w-full rounded-xl border border-slate-200 bg-slate-50 px-3 text-xs font-medium text-slate-800 outline-none transition focus:border-violet-400 focus:bg-white focus:ring-4 focus:ring-violet-100';
const money = (value: number) => `${value.toLocaleString('vi-VN')}đ`;
const branchLabel = (code: BranchCode) => code === 'Q1' ? 'Quận 1' : 'Quận 3';

function NailPreview({ design, compact = false }: { design: NailDesign; compact?: boolean }) {
  const palette = previewBackgrounds[design.preview % previewBackgrounds.length];
  return (
    <div className={`relative overflow-hidden bg-gradient-to-br from-slate-100 via-white to-violet-50 ${compact ? 'h-24' : 'h-44'}`}>
      <div className="absolute inset-x-0 bottom-0 h-2/3 bg-gradient-to-t from-slate-900/10 to-transparent" />
      <div className="absolute inset-0 flex items-end justify-center gap-1.5 px-4 pb-3">
        {[0, 1, 2, 3, 4].map((index) => {
          const base = palette[index % palette.length];
          const accent = palette[(index + 1) % palette.length];
          return <span key={index} className={`${compact ? 'h-14 w-7' : 'h-24 w-11'} relative block origin-bottom rounded-[55%_55%_28%_28%] border border-white/70 shadow-[0_8px_18px_rgba(15,23,42,0.14)]`} style={{ background: design.preview === 5 ? `linear-gradient(${125 + index * 9}deg, ${base} 0 58%, ${accent} 59% 66%, ${base} 67%)` : design.preview === 3 ? `radial-gradient(circle at ${25 + index * 12}% 28%, ${accent} 0 9%, transparent 10%), linear-gradient(145deg, ${base}, ${accent})` : `linear-gradient(${140 + index * 7}deg, ${base} 0 55%, ${accent} 56% 72%, ${palette[2]} 73%)`, transform: `rotate(${(index - 2) * 4}deg) translateY(${Math.abs(index - 2) * 4}px)` }} />;
        })}
      </div>
      <span className="absolute left-3 top-3 rounded-full bg-white/90 px-2.5 py-1 text-[10px] font-black text-slate-700 shadow-sm backdrop-blur">{design.collection}</span>
    </div>
  );
}

export default function TenantAdminNailGallery({
  searchQuery,
  onSearchQueryChange,
  selectedBranch,
  tenantName = 'Lumière Nail Studio',
  roleLabel = 'Owner · Tenant Admin',
  accessMode = 'full',
  readOnlyReason,
  onNotify
}: TenantAdminNailGalleryProps) {
  const designStorageKey = `tenant-admin-nail-designs-v1:${tenantName}`;
  const colorStorageKey = `tenant-admin-nail-colors-v1:${tenantName}`;
  const [designs, setDesigns] = useState<NailDesign[]>(() => {
    try { const value = localStorage.getItem(designStorageKey); return value ? JSON.parse(value) : designSeed; } catch { return designSeed; }
  });
  const [colors, setColors] = useState<PolishColor[]>(() => {
    try { const value = localStorage.getItem(colorStorageKey); return value ? JSON.parse(value) : colorSeed; } catch { return colorSeed; }
  });
  const [tab, setTab] = useState<LibraryTab>('DESIGNS');
  const [viewMode, setViewMode] = useState<ViewMode>('GRID');
  const [styleFilter, setStyleFilter] = useState('ALL');
  const [statusFilter, setStatusFilter] = useState('ALL');
  const [sortBy, setSortBy] = useState('POPULAR');
  const [filtersOpen, setFiltersOpen] = useState(false);
  const [selectedDesign, setSelectedDesign] = useState<NailDesign | null>(null);
  const [selectedColor, setSelectedColor] = useState<PolishColor | null>(null);
  const [formMode, setFormMode] = useState<'DESIGN' | 'COLOR' | null>(null);
  const [notice, setNotice] = useState('');
  const [formError, setFormError] = useState('');
  const [designForm, setDesignForm] = useState({ name: '', collection: '', style: 'French', level: '2', duration: '45', surcharge: '350000', status: 'DRAFT' as DesignStatus, colors: '', materials: '', technicians: '', notes: '', branchQ1: true, branchQ3: true, online: false });
  const [colorForm, setColorForm] = useState({ name: '', brand: '', code: '', hex: '#d8b4b4', finish: 'Cream · Opaque', stock: '0', minimumStock: '5', collection: '', location: '', cost: '', branchQ1: true, branchQ3: true });
  const canManage = accessMode === 'full';

  useEffect(() => { try { localStorage.setItem(designStorageKey, JSON.stringify(designs)); } catch { /* optional */ } }, [designStorageKey, designs]);
  useEffect(() => { try { localStorage.setItem(colorStorageKey, JSON.stringify(colors)); } catch { /* optional */ } }, [colorStorageKey, colors]);

  const requireManage = () => {
    if (canManage) return true;
    const message = readOnlyReason || 'Gói hiện tại chỉ cho phép xem thư viện màu và mẫu Nail.';
    setNotice(message); onNotify?.(message); return false;
  };

  const branchDesigns = useMemo(() => designs.filter((item) => selectedBranch === 'ALL' || item.branches.includes(selectedBranch as BranchCode)), [designs, selectedBranch]);
  const branchColors = useMemo(() => colors.filter((item) => selectedBranch === 'ALL' || item.branches.includes(selectedBranch as BranchCode)), [colors, selectedBranch]);
  const styles = useMemo(() => Array.from(new Set(branchDesigns.flatMap((item) => item.styles))).sort((a, b) => a.localeCompare(b, 'vi')), [branchDesigns]);
  const filteredDesigns = useMemo(() => {
    const query = searchQuery.trim().toLocaleLowerCase('vi');
    return branchDesigns.filter((item) => styleFilter === 'ALL' || item.styles.includes(styleFilter)).filter((item) => statusFilter === 'ALL' || item.status === statusFilter).filter((item) => !query || `${item.id} ${item.name} ${item.collection} ${item.styles.join(' ')} ${item.colors.map((color) => color.name).join(' ')}`.toLocaleLowerCase('vi').includes(query)).sort((a, b) => sortBy === 'NEWEST' ? b.id.localeCompare(a.id) : sortBy === 'SURCHARGE_HIGH' ? b.surcharge - a.surcharge : sortBy === 'DURATION' ? a.duration - b.duration : b.bookings - a.bookings);
  }, [branchDesigns, searchQuery, sortBy, statusFilter, styleFilter]);
  const filteredColors = useMemo(() => {
    const query = searchQuery.trim().toLocaleLowerCase('vi');
    return branchColors.filter((item) => statusFilter === 'ALL' || item.status === statusFilter).filter((item) => styleFilter === 'ALL' || item.finish.includes(styleFilter)).filter((item) => !query || `${item.id} ${item.name} ${item.brand} ${item.code} ${item.finish} ${item.collection}`.toLocaleLowerCase('vi').includes(query)).sort((a, b) => sortBy === 'NEWEST' ? b.updatedAt.localeCompare(a.updatedAt) : sortBy === 'STOCK_LOW' ? a.stock - b.stock : sortBy === 'USAGE' || sortBy === 'POPULAR' ? b.monthlyUsage - a.monthlyUsage : a.name.localeCompare(b.name, 'vi'));
  }, [branchColors, searchQuery, sortBy, statusFilter, styleFilter]);

  const activeDesigns = branchDesigns.filter((item) => item.status === 'ACTIVE' || item.status === 'TRENDING').length;
  const unavailableColors = branchColors.filter((item) => item.status !== 'ACTIVE').length;
  const totalBookings = branchDesigns.reduce((sum, item) => sum + item.bookings, 0);
  const averageSurcharge = branchDesigns.length ? Math.round(branchDesigns.reduce((sum, item) => sum + item.surcharge, 0) / branchDesigns.length / 10000) * 10000 : 0;
  const activeFilterCount = [styleFilter !== 'ALL', statusFilter !== 'ALL'].filter(Boolean).length;

  const openDesignForm = (design?: NailDesign) => {
    if (!requireManage()) return;
    if (design) setDesignForm({ name: design.name, collection: design.collection, style: design.styles[0], level: String(design.level), duration: String(design.duration), surcharge: String(design.surcharge), status: design.status, colors: design.colors.map((color) => color.name).join(', '), materials: design.materials.join(', '), technicians: design.technicians.join(', '), notes: design.notes, branchQ1: design.branches.includes('Q1'), branchQ3: design.branches.includes('Q3'), online: design.online });
    else setDesignForm({ name: '', collection: '', style: 'French', level: '2', duration: '45', surcharge: '350000', status: 'DRAFT', colors: '', materials: '', technicians: '', notes: '', branchQ1: selectedBranch !== 'Q3', branchQ3: selectedBranch !== 'Q1', online: false });
    setFormError(''); setFormMode('DESIGN');
  };
  const openColorForm = () => { if (!requireManage()) return; setColorForm({ name: '', brand: '', code: '', hex: '#d8b4b4', finish: 'Cream · Opaque', stock: '0', minimumStock: '5', collection: '', location: '', cost: '', branchQ1: selectedBranch !== 'Q3', branchQ3: selectedBranch !== 'Q1' }); setFormError(''); setFormMode('COLOR'); };

  const submitDesign = (event: FormEvent) => {
    event.preventDefault(); if (!requireManage()) return;
    if (!designForm.name.trim() || !designForm.collection.trim() || Number(designForm.duration) < 15 || Number(designForm.surcharge) < 0) { setFormError('Vui lòng nhập tên, bộ sưu tập, thời lượng và giá phụ thu hợp lệ.'); return; }
    if (!designForm.branchQ1 && !designForm.branchQ3) { setFormError('Mẫu Nail cần áp dụng tại ít nhất một chi nhánh.'); return; }
    const branches = [designForm.branchQ1 ? 'Q1' : null, designForm.branchQ3 ? 'Q3' : null].filter(Boolean) as BranchCode[];
    if (selectedDesign) {
      const updated: NailDesign = { ...selectedDesign, name: designForm.name.trim(), collection: designForm.collection.trim(), styles: [designForm.style], level: Number(designForm.level) as NailDesign['level'], duration: Number(designForm.duration), surcharge: Number(designForm.surcharge), status: designForm.status, branches, materials: designForm.materials.split(',').map((item) => item.trim()).filter(Boolean), technicians: designForm.technicians.split(',').map((item) => item.trim()).filter(Boolean), online: designForm.online, notes: designForm.notes.trim(), updatedAt: '20/07/2026 · vừa xong' };
      setDesigns((current) => current.map((item) => item.id === updated.id ? updated : item)); setSelectedDesign(updated); setNotice(`Đã cập nhật mẫu ${updated.name}.`);
    } else {
      const colorNames = designForm.colors.split(',').map((item) => item.trim()).filter(Boolean);
      const created: NailDesign = { id: `NAIL-${200 + designs.length}`, name: designForm.name.trim(), collection: designForm.collection.trim(), styles: [designForm.style], level: Number(designForm.level) as NailDesign['level'], duration: Number(designForm.duration), surcharge: Number(designForm.surcharge), status: designForm.status, branches, colors: colorNames.map((name, index) => ({ name, hex: ['#ead8d0', '#d6c7e5', '#b9c8d8'][index % 3], code: 'MỚI' })), materials: designForm.materials.split(',').map((item) => item.trim()).filter(Boolean), technicians: designForm.technicians.split(',').map((item) => item.trim()).filter(Boolean), bookings: 0, rating: 0, revenue: 0, online: designForm.online, updatedAt: '20/07/2026 · vừa xong', notes: designForm.notes.trim(), preview: designs.length % previewBackgrounds.length };
      setDesigns((current) => [created, ...current]); setSelectedDesign(created); setNotice(`Đã tạo mẫu ${created.name}.`);
    }
    setFormMode(null);
  };

  const submitColor = (event: FormEvent) => {
    event.preventDefault(); if (!requireManage()) return;
    if (!colorForm.name.trim() || !colorForm.brand.trim() || !colorForm.code.trim()) { setFormError('Vui lòng nhập tên màu, thương hiệu và mã màu.'); return; }
    if (!colorForm.branchQ1 && !colorForm.branchQ3) { setFormError('Màu sơn cần được quản lý tại ít nhất một chi nhánh.'); return; }
    const stock = Math.max(0, Number(colorForm.stock) || 0); const minimumStock = Math.max(0, Number(colorForm.minimumStock) || 0);
    const created: PolishColor = { id: `CLR-${colorForm.brand.slice(0, 3).toUpperCase()}-${colorForm.code.toUpperCase()}`, name: colorForm.name.trim(), brand: colorForm.brand.trim(), code: colorForm.code.trim(), hex: colorForm.hex, finish: colorForm.finish, stock, minimumStock, monthlyUsage: 0, linkedDesigns: 0, status: stock === 0 ? 'OUT' : stock <= minimumStock ? 'LOW' : 'ACTIVE', branches: [colorForm.branchQ1 ? 'Q1' : null, colorForm.branchQ3 ? 'Q3' : null].filter(Boolean) as BranchCode[], collection: colorForm.collection.trim() || 'Chưa phân loại', location: colorForm.location.trim() || 'Chưa xếp kệ', cost: Math.max(0, Number(colorForm.cost) || 0), updatedAt: '20/07/2026' };
    setColors((current) => [created, ...current]); setSelectedColor(created); setFormMode(null); setNotice(`Đã thêm màu ${created.brand} ${created.name}.`);
  };

  const updateDesignStatus = (design: NailDesign) => {
    if (!requireManage()) return; const next = design.status === 'HIDDEN' ? 'ACTIVE' : 'HIDDEN'; const updated = { ...design, status: next as DesignStatus, online: next !== 'HIDDEN' && design.online };
    setDesigns((current) => current.map((item) => item.id === design.id ? updated : item)); setSelectedDesign(updated); setNotice(next === 'HIDDEN' ? `Đã ẩn mẫu ${design.name}.` : `Đã mở bán lại mẫu ${design.name}.`);
  };

  const exportLibrary = () => {
    const rows = tab === 'DESIGNS' ? filteredDesigns.map((item) => [item.id, item.name, item.collection, item.styles.join(' · '), `Cấp ${item.level}`, item.duration, item.surcharge, designStatusMeta[item.status].label]) : filteredColors.map((item) => [item.id, item.brand, item.name, item.code, item.finish, item.stock, item.minimumStock, colorStatusMeta[item.status].label]);
    const header = tab === 'DESIGNS' ? ['Mã', 'Tên mẫu', 'Bộ sưu tập', 'Phong cách', 'Độ khó', 'Thời lượng', 'Phụ thu', 'Trạng thái'] : ['Mã', 'Thương hiệu', 'Tên màu', 'Mã màu', 'Hiệu ứng', 'Tồn kho', 'Tồn tối thiểu', 'Trạng thái'];
    const csv = [header, ...rows].map((row) => row.map((cell) => `"${String(cell).replace(/"/g, '""')}"`).join(',')).join('\n'); const link = document.createElement('a'); link.href = URL.createObjectURL(new Blob(['\ufeff' + csv], { type: 'text/csv;charset=utf-8' })); link.download = tab === 'DESIGNS' ? 'thu-vien-mau-nail.csv' : 'bang-mau-son.csv'; link.click(); URL.revokeObjectURL(link.href); setNotice('Đã xuất dữ liệu theo bộ lọc hiện tại.');
  };

  const switchTab = (next: LibraryTab) => { setTab(next); setStyleFilter('ALL'); setStatusFilter('ALL'); setSortBy('POPULAR'); onSearchQueryChange(''); };
  const showNotice = notice || (accessMode !== 'full' ? (readOnlyReason || 'Bạn đang xem thư viện ở chế độ chỉ đọc theo quyền của gói.') : '');

  return (
    <div className="space-y-5">
      {showNotice && <div className="flex items-start justify-between gap-3 rounded-2xl border border-violet-200 bg-violet-50 p-4 text-violet-800"><div className="flex items-start gap-3"><span className="flex h-9 w-9 shrink-0 items-center justify-center rounded-xl bg-white text-violet-600 shadow-sm"><Check className="h-4 w-4" /></span><div><p className="text-xs font-black">Cập nhật thư viện</p><p className="mt-1 text-xs leading-5">{showNotice}</p></div></div>{notice && <button type="button" onClick={() => setNotice('')} aria-label="Đóng thông báo" className="flex h-8 w-8 items-center justify-center border-0 bg-transparent p-0 text-violet-500 shadow-none"><X className="h-4 w-4" /></button>}</div>}

      <section className="flex flex-col gap-5 xl:flex-row xl:items-end xl:justify-between">
        <div><div className="mb-2 flex items-center gap-2 text-xs font-black uppercase tracking-[0.12em] text-violet-600"><Palette className="h-4 w-4" />Danh mục sáng tạo</div><h1 className="text-2xl font-black tracking-[-0.035em] text-slate-950 sm:text-3xl">Màu & mẫu Nail</h1><p className="mt-2 max-w-3xl text-sm leading-6 text-slate-500">Quản lý thư viện thiết kế, mã màu, vật liệu, phụ thu và khả năng thực hiện tại từng chi nhánh của {tenantName}.</p></div>
        <div className="flex flex-col gap-2 sm:flex-row"><button type="button" onClick={exportLibrary} className="flex h-11 items-center justify-center gap-2 border border-slate-200 bg-white px-4 text-xs font-bold text-slate-600 shadow-sm"><Download className="h-4 w-4" />Xuất dữ liệu</button><button type="button" onClick={openColorForm} disabled={!canManage} className="flex h-11 items-center justify-center gap-2 border border-violet-200 bg-white px-4 text-xs font-black text-violet-700 shadow-sm"><Palette className="h-4 w-4" />Thêm màu sơn</button><button type="button" onClick={() => openDesignForm()} disabled={!canManage} className="flex h-11 items-center justify-center gap-2 border border-violet-700 bg-violet-600 px-5 text-xs font-black text-white shadow-lg shadow-violet-200"><Plus className="h-4 w-4" />Thêm mẫu Nail</button></div>
      </section>

      <section className="grid gap-3 sm:grid-cols-2 xl:grid-cols-4">
        {[{ label: 'Mẫu đang mở bán', value: activeDesigns.toLocaleString('vi-VN'), detail: `${branchDesigns.length - activeDesigns} bản nháp hoặc đang ẩn`, icon: Sparkles, tone: 'bg-violet-50 text-violet-600' }, { label: 'Mã màu trong thư viện', value: branchColors.length.toLocaleString('vi-VN'), detail: `${unavailableColors} màu sắp hết hoặc hết hàng`, icon: Palette, tone: 'bg-blue-50 text-blue-600' }, { label: 'Lượt chọn trong tháng', value: totalBookings.toLocaleString('vi-VN'), detail: '+18,6% so với tháng trước', icon: TrendingUp, tone: 'bg-emerald-50 text-emerald-600' }, { label: 'Phụ thu trung bình', value: money(averageSurcharge), detail: 'Theo các mẫu trong phạm vi', icon: CircleDollarSign, tone: 'bg-amber-50 text-amber-600' }].map(({ label, value, detail, icon: Icon, tone }) => <article key={label} className="rounded-2xl border border-slate-200 bg-white p-4 shadow-[0_10px_30px_rgba(15,23,42,0.04)]"><div className="flex items-start justify-between gap-3"><div><p className="text-xs font-bold text-slate-500">{label}</p><p className="mt-1.5 text-xl font-black tracking-tight text-slate-950">{value}</p></div><span className={`flex h-10 w-10 shrink-0 items-center justify-center rounded-xl ${tone}`}><Icon className="h-5 w-5" /></span></div><p className="mt-2 text-xs font-semibold text-slate-400">{detail}</p></article>)}
      </section>

      <section className="overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-[0_10px_30px_rgba(15,23,42,0.04)]">
        <div className="border-b border-slate-100 p-4 sm:px-5"><div className="flex flex-col gap-4 lg:flex-row lg:items-center lg:justify-between"><div className="inline-flex w-fit rounded-xl bg-slate-100 p-1"><button type="button" onClick={() => switchTab('DESIGNS')} className={`flex h-9 items-center gap-2 border-0 px-4 text-xs font-black shadow-none ${tab === 'DESIGNS' ? 'bg-white text-violet-700 shadow-sm' : 'bg-transparent text-slate-500'}`}><Image className="h-4 w-4" />Mẫu thiết kế <span className="rounded-full bg-violet-50 px-2 py-0.5 text-[10px] text-violet-700">{branchDesigns.length}</span></button><button type="button" onClick={() => switchTab('COLORS')} className={`flex h-9 items-center gap-2 border-0 px-4 text-xs font-black shadow-none ${tab === 'COLORS' ? 'bg-white text-violet-700 shadow-sm' : 'bg-transparent text-slate-500'}`}><Palette className="h-4 w-4" />Bảng màu <span className="rounded-full bg-violet-50 px-2 py-0.5 text-[10px] text-violet-700">{branchColors.length}</span></button></div><div className="flex flex-wrap items-center gap-2"><button type="button" onClick={() => setFiltersOpen((value) => !value)} className={`flex h-10 items-center gap-2 border px-3 text-xs font-bold shadow-sm ${filtersOpen || activeFilterCount ? 'border-violet-200 bg-violet-50 text-violet-700' : 'border-slate-200 bg-white text-slate-600'}`}><Filter className="h-4 w-4" />Bộ lọc{activeFilterCount > 0 && <span className="rounded-full bg-violet-600 px-1.5 py-0.5 text-[10px] text-white">{activeFilterCount}</span>}</button><div className="flex rounded-xl border border-slate-200 bg-white p-1"><button type="button" onClick={() => setViewMode('GRID')} aria-label="Xem dạng lưới" className={`flex h-8 w-8 items-center justify-center border-0 p-0 shadow-none ${viewMode === 'GRID' ? 'bg-slate-100 text-violet-700' : 'bg-transparent text-slate-400'}`}><LayoutGrid className="h-4 w-4" /></button><button type="button" onClick={() => setViewMode('LIST')} aria-label="Xem dạng danh sách" className={`flex h-8 w-8 items-center justify-center border-0 p-0 shadow-none ${viewMode === 'LIST' ? 'bg-slate-100 text-violet-700' : 'bg-transparent text-slate-400'}`}><LayoutList className="h-4 w-4" /></button></div></div></div>
          {filtersOpen && <div className="mt-4 grid gap-3 rounded-xl bg-slate-50 p-3 sm:grid-cols-2 lg:grid-cols-4"><div className="relative"><Search className="absolute left-3 top-1/2 h-4 w-4 -translate-y-1/2 text-slate-400" /><input value={searchQuery} onChange={(event) => onSearchQueryChange(event.target.value)} placeholder={tab === 'DESIGNS' ? 'Tên mẫu, màu, bộ sưu tập...' : 'Mã màu, thương hiệu...'} className={`${inputClass} pl-10`} /></div><BeautifulSelect value={styleFilter} onChange={(event) => setStyleFilter(event.target.value)} className={inputClass}><option value="ALL">Tất cả {tab === 'DESIGNS' ? 'phong cách' : 'hiệu ứng'}</option>{tab === 'DESIGNS' ? styles.map((style) => <option key={style} value={style}>{style}</option>) : ['Cream', 'Sheer', 'Chrome', 'Jelly', 'Metallic'].map((finish) => <option key={finish} value={finish}>{finish}</option>)}</BeautifulSelect><BeautifulSelect value={statusFilter} onChange={(event) => setStatusFilter(event.target.value)} className={inputClass}><option value="ALL">Tất cả trạng thái</option>{tab === 'DESIGNS' ? Object.entries(designStatusMeta).map(([value, meta]) => <option key={value} value={value}>{meta.label}</option>) : Object.entries(colorStatusMeta).map(([value, meta]) => <option key={value} value={value}>{meta.label}</option>)}</BeautifulSelect><BeautifulSelect value={sortBy} onChange={(event) => setSortBy(event.target.value)} className={inputClass}>{tab === 'DESIGNS' ? <><option value="POPULAR">Lượt chọn nhiều nhất</option><option value="NEWEST">Mới cập nhật</option><option value="SURCHARGE_HIGH">Phụ thu cao nhất</option><option value="DURATION">Thời gian ngắn nhất</option></> : <><option value="POPULAR">Dùng nhiều nhất</option><option value="STOCK_LOW">Tồn kho thấp nhất</option><option value="NEWEST">Mới cập nhật</option><option value="NAME">Tên A–Z</option></>}</BeautifulSelect></div>}
        </div>

        <div className="p-4 sm:p-5">
          <div className="mb-4 flex items-center justify-between gap-3"><div><p className="text-sm font-black text-slate-900">{tab === 'DESIGNS' ? `${filteredDesigns.length} mẫu phù hợp` : `${filteredColors.length} màu phù hợp`}</p><p className="mt-1 text-xs text-slate-400">Phạm vi: {selectedBranch === 'ALL' ? 'Tất cả chi nhánh' : `Chi nhánh ${branchLabel(selectedBranch as BranchCode)}`} · Quyền: {roleLabel}</p></div>{activeFilterCount > 0 && <button type="button" onClick={() => { setStyleFilter('ALL'); setStatusFilter('ALL'); }} className="h-8 border-0 bg-transparent px-2 text-xs font-black text-violet-600 shadow-none">Xóa bộ lọc</button>}</div>
          {tab === 'DESIGNS' ? (viewMode === 'GRID' ? <div className="grid gap-4 md:grid-cols-2 xl:grid-cols-3">{filteredDesigns.map((design) => <button key={design.id} type="button" onClick={() => setSelectedDesign(design)} className="group h-auto overflow-hidden border border-slate-200 bg-white p-0 text-left shadow-sm hover:border-violet-200 hover:shadow-lg"><NailPreview design={design} /><div className="p-4"><div className="flex items-start justify-between gap-3"><div className="min-w-0"><p className="text-[10px] font-black uppercase tracking-wide text-violet-600">{design.id}</p><h3 className="mt-1 truncate text-base font-black text-slate-900">{design.name}</h3></div><span className={`shrink-0 rounded-full px-2.5 py-1 text-[10px] font-bold ring-1 ${designStatusMeta[design.status].badge}`}>{designStatusMeta[design.status].label}</span></div><div className="mt-3 flex flex-wrap gap-1.5">{design.styles.map((style) => <span key={style} className="rounded-lg bg-slate-100 px-2 py-1 text-[10px] font-bold text-slate-600">{style}</span>)}</div><div className="mt-4 grid grid-cols-3 gap-2 border-t border-slate-100 pt-3"><div><p className="text-[10px] text-slate-400">Độ khó</p><p className="mt-1 text-xs font-black text-slate-700">Cấp {design.level}</p></div><div><p className="text-[10px] text-slate-400">Thời lượng</p><p className="mt-1 text-xs font-black text-slate-700">+{design.duration} phút</p></div><div><p className="text-[10px] text-slate-400">Phụ thu</p><p className="mt-1 text-xs font-black text-violet-700">{money(design.surcharge)}</p></div></div><div className="mt-3 flex items-center justify-between"><div className="flex -space-x-1.5">{design.colors.map((color) => <span key={color.code} title={`${color.name} · ${color.code}`} className="h-6 w-6 rounded-full border-2 border-white shadow-sm" style={{ backgroundColor: color.hex }} />)}</div><span className="flex items-center gap-1 text-xs font-black text-amber-600"><Star className="h-3.5 w-3.5 fill-amber-400" />{design.rating || 'Mới'} <span className="font-medium text-slate-400">· {design.bookings} lượt</span></span></div></div></button>)}</div> : <div className="divide-y divide-slate-100 overflow-hidden rounded-2xl border border-slate-200">{filteredDesigns.map((design) => <button key={design.id} type="button" onClick={() => setSelectedDesign(design)} className="grid h-auto w-full items-center gap-3 rounded-none border-0 bg-white p-3 text-left shadow-none hover:bg-slate-50 sm:grid-cols-[120px_1.3fr_1fr_90px_110px]"><div className="overflow-hidden rounded-xl"><NailPreview design={design} compact /></div><div className="min-w-0"><p className="text-[10px] font-black text-violet-600">{design.id}</p><p className="mt-1 truncate text-sm font-black text-slate-900">{design.name}</p><p className="mt-1 truncate text-xs text-slate-400">{design.collection}</p></div><div><div className="flex flex-wrap gap-1">{design.styles.map((style) => <span key={style} className="rounded bg-slate-100 px-1.5 py-1 text-[10px] font-bold text-slate-600">{style}</span>)}</div><p className="mt-2 text-xs text-slate-400">Cấp {design.level} · +{design.duration} phút</p></div><p className="text-xs font-black text-violet-700">{money(design.surcharge)}</p><span className={`w-fit rounded-full px-2 py-1 text-[10px] font-bold ring-1 ${designStatusMeta[design.status].badge}`}>{designStatusMeta[design.status].label}</span></button>)}</div>) : <div className={viewMode === 'GRID' ? 'grid gap-3 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4' : 'divide-y divide-slate-100 overflow-hidden rounded-2xl border border-slate-200'}>{filteredColors.map((color) => viewMode === 'GRID' ? <button key={color.id} type="button" onClick={() => setSelectedColor(color)} className="h-auto border border-slate-200 bg-white p-4 text-left shadow-sm hover:border-violet-200 hover:shadow-lg"><div className="flex items-start justify-between"><span className="h-16 w-16 rounded-2xl border-4 border-white shadow-[0_6px_18px_rgba(15,23,42,0.16)] ring-1 ring-slate-200" style={{ backgroundColor: color.hex }} /><span className={`rounded-full px-2 py-1 text-[10px] font-bold ring-1 ${colorStatusMeta[color.status].badge}`}>{colorStatusMeta[color.status].label}</span></div><p className="mt-4 text-[10px] font-black uppercase tracking-wide text-slate-400">{color.brand} · {color.code}</p><h3 className="mt-1 text-base font-black text-slate-900">{color.name}</h3><p className="mt-1 text-xs text-slate-500">{color.finish}</p><div className="mt-4 flex items-center justify-between border-t border-slate-100 pt-3"><div><p className="text-[10px] text-slate-400">Tồn kho</p><p className="mt-1 text-sm font-black text-slate-800">{color.stock} chai</p></div><div className="text-right"><p className="text-[10px] text-slate-400">Mẫu liên kết</p><p className="mt-1 text-sm font-black text-violet-700">{color.linkedDesigns}</p></div></div></button> : <button key={color.id} type="button" onClick={() => setSelectedColor(color)} className="grid h-auto w-full items-center gap-3 rounded-none border-0 bg-white p-3 text-left shadow-none hover:bg-slate-50 sm:grid-cols-[54px_1.2fr_1fr_90px_110px]"><span className="h-11 w-11 rounded-xl border-2 border-white shadow ring-1 ring-slate-200" style={{ backgroundColor: color.hex }} /><div><p className="text-xs font-black text-slate-900">{color.name}</p><p className="mt-1 text-[10px] text-slate-400">{color.brand} · {color.code}</p></div><p className="text-xs font-bold text-slate-600">{color.finish}</p><p className="text-xs font-black text-slate-800">{color.stock} chai</p><span className={`w-fit rounded-full px-2 py-1 text-[10px] font-bold ring-1 ${colorStatusMeta[color.status].badge}`}>{colorStatusMeta[color.status].label}</span></button>)}</div>}
          {((tab === 'DESIGNS' && filteredDesigns.length === 0) || (tab === 'COLORS' && filteredColors.length === 0)) && <div className="py-20 text-center"><span className="mx-auto flex h-14 w-14 items-center justify-center rounded-2xl bg-slate-100 text-slate-400">{tab === 'DESIGNS' ? <Image className="h-6 w-6" /> : <Palette className="h-6 w-6" />}</span><p className="mt-4 text-sm font-black text-slate-800">Không tìm thấy dữ liệu phù hợp</p><p className="mt-1 text-xs text-slate-400">Thử đổi từ khóa hoặc xóa bộ lọc đang áp dụng.</p></div>}
        </div>
      </section>

      <section className="grid gap-4 lg:grid-cols-3"><article className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><div className="flex items-start justify-between"><div><h2 className="text-sm font-black text-slate-900">Xu hướng thiết kế</h2><p className="mt-1 text-xs text-slate-400">30 ngày gần nhất</p></div><BarChart3 className="h-5 w-5 text-violet-500" /></div><div className="mt-4 space-y-3">{[{ label: 'Chrome & Pearl', value: 86, detail: '+38%' }, { label: 'Milky Nude', value: 70, detail: '+24%' }, { label: 'Cat Eye', value: 58, detail: '+18%' }].map((item) => <div key={item.label}><div className="mb-1.5 flex justify-between text-xs"><span className="font-bold text-slate-600">{item.label}</span><span className="font-black text-violet-700">{item.detail}</span></div><div className="h-2 rounded-full bg-slate-100"><div className="h-full rounded-full bg-gradient-to-r from-violet-500 to-fuchsia-400" style={{ width: `${item.value}%` }} /></div></div>)}</div></article><article className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><div className="flex items-start justify-between"><div><h2 className="text-sm font-black text-slate-900">Chất lượng thư viện</h2><p className="mt-1 text-xs text-slate-400">Mức độ sẵn sàng để bán</p></div><PackageCheck className="h-5 w-5 text-emerald-500" /></div><div className="mt-4 grid grid-cols-3 gap-2 text-center"><div className="rounded-xl bg-emerald-50 p-3"><p className="text-xl font-black text-emerald-700">92%</p><p className="mt-1 text-[10px] font-bold text-emerald-600">Có đủ màu</p></div><div className="rounded-xl bg-blue-50 p-3"><p className="text-xl font-black text-blue-700">84%</p><p className="mt-1 text-[10px] font-bold text-blue-600">Đủ KTV</p></div><div className="rounded-xl bg-violet-50 p-3"><p className="text-xl font-black text-violet-700">76%</p><p className="mt-1 text-[10px] font-bold text-violet-600">Đã mở online</p></div></div><p className="mt-4 rounded-xl bg-slate-50 p-3 text-xs leading-5 text-slate-500">8 mẫu mới cần gắn thêm kỹ thuật viên trước khi mở bán đồng thời tại hai chi nhánh.</p></article><article className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><div className="flex items-start justify-between"><div><h2 className="text-sm font-black text-slate-900">Việc cần xử lý</h2><p className="mt-1 text-xs text-slate-400">Ưu tiên của Tenant Admin</p></div><Tag className="h-5 w-5 text-amber-500" /></div><div className="mt-3 divide-y divide-slate-100">{['Duyệt ảnh mẫu Ocean Glass 3D', `Tạo đề xuất nhập ${unavailableColors} màu`, 'Gắn KTV cho bộ sưu tập tháng 8'].map((item, index) => <button key={item} type="button" className="flex h-auto w-full items-center gap-3 rounded-none border-0 bg-white py-3 text-left shadow-none"><span className={`flex h-7 w-7 shrink-0 items-center justify-center rounded-lg text-xs font-black ${index === 0 ? 'bg-rose-50 text-rose-600' : 'bg-slate-100 text-slate-500'}`}>{index + 1}</span><span className="flex-1 text-xs font-bold text-slate-700">{item}</span><ChevronRight className="h-4 w-4 text-slate-300" /></button>)}</div></article></section>

      {selectedDesign && <div className="fixed inset-0 z-[70] flex justify-end bg-slate-950/45 backdrop-blur-[2px]"><button type="button" aria-label="Đóng chi tiết mẫu Nail" onClick={() => setSelectedDesign(null)} className="absolute inset-0 min-h-0 rounded-none border-0 bg-transparent p-0 shadow-none" /><aside className="relative flex h-full w-full max-w-[560px] flex-col overflow-hidden bg-white shadow-2xl"><div className="flex items-start justify-between border-b border-slate-100 px-5 py-5 sm:px-6"><div><div className="flex flex-wrap items-center gap-2"><span className="text-[10px] font-black uppercase tracking-wide text-violet-600">{selectedDesign.id}</span><span className={`rounded-full px-2 py-0.5 text-[10px] font-bold ring-1 ${designStatusMeta[selectedDesign.status].badge}`}>{designStatusMeta[selectedDesign.status].label}</span>{selectedDesign.online && <span className="rounded-full bg-blue-50 px-2 py-0.5 text-[10px] font-bold text-blue-700 ring-1 ring-blue-200">Đang hiển thị online</span>}</div><h2 className="mt-2 text-xl font-black text-slate-900">{selectedDesign.name}</h2><p className="mt-1 text-xs text-slate-400">{selectedDesign.collection}</p></div><button type="button" onClick={() => setSelectedDesign(null)} aria-label="Đóng" className="flex h-9 w-9 shrink-0 items-center justify-center border border-slate-200 bg-white p-0 text-slate-500 shadow-sm"><X className="h-4 w-4" /></button></div><div className="flex-1 overflow-y-auto"><NailPreview design={selectedDesign} /><div className="space-y-5 p-5 sm:p-6"><div className="grid grid-cols-4 gap-2"><div className="rounded-xl bg-slate-50 p-3"><Clock3 className="h-4 w-4 text-blue-500" /><p className="mt-2 text-[10px] text-slate-400">Thời lượng thêm</p><p className="mt-1 text-sm font-black text-slate-900">{selectedDesign.duration} phút</p></div><div className="rounded-xl bg-violet-50 p-3"><CircleDollarSign className="h-4 w-4 text-violet-500" /><p className="mt-2 text-[10px] text-violet-400">Phụ thu</p><p className="mt-1 text-sm font-black text-violet-800">{money(selectedDesign.surcharge)}</p></div><div className="rounded-xl bg-amber-50 p-3"><Star className="h-4 w-4 fill-amber-400 text-amber-500" /><p className="mt-2 text-[10px] text-amber-500">Đánh giá</p><p className="mt-1 text-sm font-black text-amber-800">{selectedDesign.rating || '—'}/5</p></div><div className="rounded-xl bg-emerald-50 p-3"><TrendingUp className="h-4 w-4 text-emerald-500" /><p className="mt-2 text-[10px] text-emerald-500">Lượt chọn</p><p className="mt-1 text-sm font-black text-emerald-800">{selectedDesign.bookings}</p></div></div><section><div className="flex items-center gap-2"><Palette className="h-4 w-4 text-violet-600" /><h3 className="text-sm font-black text-slate-900">Màu sơn sử dụng</h3></div><div className="mt-3 grid gap-2 sm:grid-cols-3">{selectedDesign.colors.map((color) => <div key={color.code} className="flex items-center gap-3 rounded-xl border border-slate-200 p-3"><span className="h-9 w-9 shrink-0 rounded-xl border-2 border-white shadow ring-1 ring-slate-200" style={{ backgroundColor: color.hex }} /><div className="min-w-0"><p className="truncate text-xs font-black text-slate-700">{color.name}</p><p className="mt-1 text-[10px] text-slate-400">{color.code}</p></div></div>)}</div></section><section className="grid gap-4 sm:grid-cols-2"><div className="rounded-2xl border border-slate-200 p-4"><div className="flex items-center gap-2"><Layers3 className="h-4 w-4 text-fuchsia-500" /><h3 className="text-xs font-black text-slate-800">Vật liệu & phụ kiện</h3></div><div className="mt-3 space-y-2">{selectedDesign.materials.map((item) => <div key={item} className="flex items-center gap-2 text-xs text-slate-600"><Check className="h-3.5 w-3.5 text-emerald-500" />{item}</div>)}</div></div><div className="rounded-2xl border border-slate-200 p-4"><div className="flex items-center gap-2"><UserRoundCheck className="h-4 w-4 text-blue-500" /><h3 className="text-xs font-black text-slate-800">Kỹ thuật viên đủ kỹ năng</h3></div><div className="mt-3 flex flex-wrap gap-2">{selectedDesign.technicians.length ? selectedDesign.technicians.map((item) => <span key={item} className="rounded-lg bg-blue-50 px-2 py-1.5 text-[10px] font-bold text-blue-700">{item}</span>) : <span className="text-xs text-rose-600">Chưa gắn kỹ thuật viên</span>}</div></div></section><section className="rounded-2xl bg-slate-950 p-5 text-white"><div className="flex items-start justify-between"><div><p className="text-[10px] font-bold uppercase tracking-wide text-slate-400">Hiệu suất trong tháng</p><p className="mt-2 text-2xl font-black">{money(selectedDesign.revenue)}</p><p className="mt-1 text-xs text-slate-400">Doanh thu phụ thu từ {selectedDesign.bookings} lượt chọn</p></div><ArrowUpRight className="h-5 w-5 text-emerald-300" /></div><div className="mt-4 grid grid-cols-3 gap-3 border-t border-white/10 pt-4"><div><p className="text-[10px] text-slate-500">Giá trị/lượt</p><p className="mt-1 text-xs font-black">{money(selectedDesign.bookings ? Math.round(selectedDesign.revenue / selectedDesign.bookings) : selectedDesign.surcharge)}</p></div><div><p className="text-[10px] text-slate-500">Độ khó</p><p className="mt-1 text-xs font-black">Cấp {selectedDesign.level}/4</p></div><div><p className="text-[10px] text-slate-500">Chi nhánh</p><p className="mt-1 text-xs font-black">{selectedDesign.branches.map(branchLabel).join(' · ')}</p></div></div></section><section className="rounded-2xl bg-amber-50 p-4"><p className="text-[10px] font-black uppercase tracking-wide text-amber-700">Lưu ý tư vấn & vận hành</p><p className="mt-2 text-xs leading-5 text-amber-800">{selectedDesign.notes || 'Chưa có lưu ý cho mẫu này.'}</p></section><p className="text-[10px] text-slate-400">Cập nhật gần nhất {selectedDesign.updatedAt} · Mọi thay đổi được ghi nhận theo tài khoản {roleLabel}.</p></div></div><div className="border-t border-slate-100 bg-slate-50 p-4 sm:px-6"><div className="flex gap-2"><button type="button" onClick={() => openDesignForm(selectedDesign)} disabled={!canManage} className="flex h-11 items-center justify-center gap-2 border border-slate-200 bg-white px-4 text-xs font-bold text-slate-600 shadow-sm"><Pencil className="h-4 w-4" />Chỉnh sửa</button><button type="button" onClick={() => updateDesignStatus(selectedDesign)} disabled={!canManage} className="flex h-11 flex-1 items-center justify-center gap-2 border border-violet-700 bg-violet-600 px-4 text-xs font-black text-white shadow-lg shadow-violet-200">{selectedDesign.status === 'HIDDEN' ? <Eye className="h-4 w-4" /> : <EyeOff className="h-4 w-4" />}{selectedDesign.status === 'HIDDEN' ? 'Mở bán mẫu Nail' : 'Ẩn khỏi thư viện bán'}</button></div></div></aside></div>}

      {selectedColor && <div className="fixed inset-0 z-[70] flex justify-end bg-slate-950/45 backdrop-blur-[2px]"><button type="button" aria-label="Đóng chi tiết màu sơn" onClick={() => setSelectedColor(null)} className="absolute inset-0 min-h-0 rounded-none border-0 bg-transparent p-0 shadow-none" /><aside className="relative flex h-full w-full max-w-[500px] flex-col bg-white shadow-2xl"><div className="flex items-start justify-between border-b border-slate-100 p-5 sm:p-6"><div className="flex items-center gap-4"><span className="h-16 w-16 rounded-2xl border-4 border-white shadow-xl ring-1 ring-slate-200" style={{ backgroundColor: selectedColor.hex }} /><div><span className={`rounded-full px-2 py-1 text-[10px] font-bold ring-1 ${colorStatusMeta[selectedColor.status].badge}`}>{colorStatusMeta[selectedColor.status].label}</span><h2 className="mt-2 text-xl font-black text-slate-900">{selectedColor.name}</h2><p className="mt-1 text-xs text-slate-400">{selectedColor.brand} · {selectedColor.code}</p></div></div><button type="button" onClick={() => setSelectedColor(null)} aria-label="Đóng" className="flex h-9 w-9 shrink-0 items-center justify-center border border-slate-200 bg-white p-0 text-slate-500 shadow-sm"><X className="h-4 w-4" /></button></div><div className="flex-1 overflow-y-auto p-5 sm:p-6"><div className="rounded-2xl border border-slate-200 p-5"><div className="flex items-center justify-between"><div><p className="text-[10px] font-bold uppercase tracking-wide text-slate-400">Mã màu nội bộ</p><p className="mt-1 text-lg font-black text-slate-900">{selectedColor.id}</p></div><span className="rounded-lg bg-slate-950 px-3 py-2 font-mono text-xs font-black text-white">{selectedColor.hex.toUpperCase()}</span></div><div className="mt-5 grid grid-cols-2 gap-3">{[{ label: 'Hiệu ứng', value: selectedColor.finish }, { label: 'Bộ sưu tập', value: selectedColor.collection }, { label: 'Vị trí lưu', value: selectedColor.location }, { label: 'Giá nhập', value: `${money(selectedColor.cost)}/chai` }].map((item) => <div key={item.label} className="rounded-xl bg-slate-50 p-3"><p className="text-[10px] text-slate-400">{item.label}</p><p className="mt-1 text-xs font-black text-slate-700">{item.value}</p></div>)}</div></div><section className={`mt-5 rounded-2xl border p-5 ${selectedColor.status === 'OUT' ? 'border-rose-200 bg-rose-50' : selectedColor.status === 'LOW' ? 'border-amber-200 bg-amber-50' : 'border-emerald-200 bg-emerald-50'}`}><div className="flex items-start justify-between"><div><p className="text-xs font-black text-slate-800">Tồn kho khả dụng</p><p className="mt-1 text-3xl font-black text-slate-950">{selectedColor.stock} <span className="text-sm text-slate-500">chai</span></p></div><PackageCheck className="h-5 w-5 text-slate-500" /></div><div className="mt-4 h-2 overflow-hidden rounded-full bg-white/80"><div className={`h-full rounded-full ${selectedColor.status === 'OUT' ? 'bg-rose-500' : selectedColor.status === 'LOW' ? 'bg-amber-500' : 'bg-emerald-500'}`} style={{ width: `${Math.min(100, selectedColor.stock / Math.max(1, selectedColor.minimumStock) * 100)}%` }} /></div><div className="mt-3 flex justify-between text-xs font-bold text-slate-600"><span>Định mức tối thiểu: {selectedColor.minimumStock}</span><span>Dùng tháng: {selectedColor.monthlyUsage}</span></div></section><div className="mt-5 grid grid-cols-2 gap-3"><div className="rounded-2xl border border-slate-200 p-4"><p className="text-[10px] text-slate-400">Mẫu đang liên kết</p><p className="mt-2 text-2xl font-black text-violet-700">{selectedColor.linkedDesigns}</p><p className="mt-1 text-xs text-slate-500">mẫu trong thư viện</p></div><div className="rounded-2xl border border-slate-200 p-4"><p className="text-[10px] text-slate-400">Phạm vi sử dụng</p><p className="mt-2 text-sm font-black text-slate-800">{selectedColor.branches.map(branchLabel).join(' · ')}</p><p className="mt-1 text-xs text-slate-500">Theo tồn kho chi nhánh</p></div></div><section className="mt-5 rounded-2xl bg-violet-50 p-4"><p className="text-xs font-black text-violet-800">Đề xuất quản trị</p><p className="mt-2 text-xs leading-5 text-violet-700">{selectedColor.status === 'OUT' ? `Tạm ẩn ${selectedColor.linkedDesigns} mẫu đang dùng màu này hoặc chọn màu thay thế trước khi nhận lịch.` : selectedColor.status === 'LOW' ? `Tạo phiếu nhập ít nhất ${Math.max(selectedColor.minimumStock * 2 - selectedColor.stock, selectedColor.minimumStock)} chai để đảm bảo nhu cầu 30 ngày.` : 'Tồn kho đang trong ngưỡng an toàn. Tiếp tục theo dõi tốc độ sử dụng theo tuần.'}</p></section><p className="mt-5 text-[10px] text-slate-400">Cập nhật tồn kho gần nhất: {selectedColor.updatedAt} · Dữ liệu áp dụng trong phạm vi tenant.</p></div><div className="border-t border-slate-100 bg-slate-50 p-4 sm:px-6"><button type="button" onClick={() => { if (!requireManage()) return; setNotice(`Đã tạo đề xuất nhập màu ${selectedColor.name}.`); setSelectedColor(null); }} disabled={!canManage} className="flex h-11 w-full items-center justify-center gap-2 border border-violet-700 bg-violet-600 px-4 text-xs font-black text-white shadow-lg shadow-violet-200"><PackageCheck className="h-4 w-4" />Tạo đề xuất nhập hàng</button></div></aside></div>}

      {formMode && <div className="fixed inset-0 z-[80] flex items-center justify-center bg-slate-950/55 p-4 backdrop-blur-sm"><button type="button" aria-label="Đóng biểu mẫu" onClick={() => setFormMode(null)} className="absolute inset-0 min-h-0 rounded-none border-0 bg-transparent p-0 shadow-none" /><form onSubmit={formMode === 'DESIGN' ? submitDesign : submitColor} className="relative max-h-[calc(100vh-2rem)] w-full max-w-3xl overflow-y-auto rounded-3xl bg-white shadow-2xl"><div className="sticky top-0 z-10 flex items-start justify-between border-b border-slate-100 bg-white px-5 py-5 sm:px-6"><div><p className="text-[10px] font-black uppercase tracking-[0.12em] text-violet-600">{tenantName} · {roleLabel}</p><h2 className="mt-1 text-lg font-black text-slate-900">{formMode === 'DESIGN' ? (selectedDesign ? `Chỉnh sửa ${selectedDesign.id}` : 'Thêm mẫu Nail mới') : 'Thêm màu sơn mới'}</h2><p className="mt-1 text-xs text-slate-500">{formMode === 'DESIGN' ? 'Thiết lập cách hiển thị, giá phụ thu, vật liệu và khả năng phục vụ.' : 'Khai báo mã màu, hiệu ứng, tồn kho và phạm vi chi nhánh.'}</p></div><button type="button" onClick={() => setFormMode(null)} aria-label="Đóng" className="flex h-9 w-9 items-center justify-center border border-slate-200 bg-white p-0 text-slate-500 shadow-sm"><X className="h-4 w-4" /></button></div><div className="space-y-5 p-5 sm:p-6">{formError && <div className="rounded-xl bg-rose-50 p-3 text-xs font-bold text-rose-700">{formError}</div>}{formMode === 'DESIGN' ? <><fieldset><legend className="mb-3 flex items-center gap-2 text-sm font-black text-slate-800"><Sparkles className="h-4 w-4 text-violet-600" />Thông tin mẫu thiết kế</legend><div className="grid gap-3 sm:grid-cols-2"><label><span className="mb-1.5 block text-xs font-bold text-slate-600">Tên mẫu Nail *</span><input value={designForm.name} onChange={(event) => setDesignForm((current) => ({ ...current, name: event.target.value }))} className={inputClass} placeholder="Ví dụ: Crystal French Chrome" /></label><label><span className="mb-1.5 block text-xs font-bold text-slate-600">Bộ sưu tập *</span><input value={designForm.collection} onChange={(event) => setDesignForm((current) => ({ ...current, collection: event.target.value }))} className={inputClass} placeholder="Summer 2026" /></label><label><span className="mb-1.5 block text-xs font-bold text-slate-600">Phong cách chính</span><BeautifulSelect value={designForm.style} onChange={(event) => setDesignForm((current) => ({ ...current, style: event.target.value }))} className={inputClass}>{['French', 'Chrome', 'Ombre', 'Đính đá', '3D Gel', 'Minimal', 'Cat Eye'].map((item) => <option key={item}>{item}</option>)}</BeautifulSelect></label><label><span className="mb-1.5 block text-xs font-bold text-slate-600">Trạng thái</span><BeautifulSelect value={designForm.status} onChange={(event) => setDesignForm((current) => ({ ...current, status: event.target.value as DesignStatus }))} className={inputClass}>{Object.entries(designStatusMeta).map(([value, meta]) => <option key={value} value={value}>{meta.label}</option>)}</BeautifulSelect></label></div></fieldset><fieldset className="border-t border-slate-100 pt-5"><legend className="mb-3 flex items-center gap-2 text-sm font-black text-slate-800"><CircleDollarSign className="h-4 w-4 text-emerald-600" />Thời lượng & giá bán</legend><div className="grid gap-3 sm:grid-cols-3"><label><span className="mb-1.5 block text-xs font-bold text-slate-600">Độ khó</span><BeautifulSelect value={designForm.level} onChange={(event) => setDesignForm((current) => ({ ...current, level: event.target.value }))} className={inputClass}>{[1, 2, 3, 4].map((level) => <option key={level} value={level}>Cấp {level}</option>)}</BeautifulSelect></label><label><span className="mb-1.5 block text-xs font-bold text-slate-600">Thời gian thêm (phút)</span><input type="number" min="15" step="5" value={designForm.duration} onChange={(event) => setDesignForm((current) => ({ ...current, duration: event.target.value }))} className={inputClass} /></label><label><span className="mb-1.5 block text-xs font-bold text-slate-600">Giá phụ thu</span><input type="number" min="0" step="10000" value={designForm.surcharge} onChange={(event) => setDesignForm((current) => ({ ...current, surcharge: event.target.value }))} className={inputClass} /></label></div></fieldset><fieldset className="border-t border-slate-100 pt-5"><legend className="mb-3 flex items-center gap-2 text-sm font-black text-slate-800"><Layers3 className="h-4 w-4 text-fuchsia-600" />Màu, vật liệu & kỹ thuật viên</legend><div className="grid gap-3 sm:grid-cols-2"><label><span className="mb-1.5 block text-xs font-bold text-slate-600">Màu sử dụng</span><textarea value={designForm.colors} onChange={(event) => setDesignForm((current) => ({ ...current, colors: event.target.value }))} className="min-h-20 w-full rounded-xl border border-slate-200 bg-slate-50 p-3 text-xs outline-none focus:border-violet-400 focus:bg-white focus:ring-4 focus:ring-violet-100" placeholder="Bubble Bath, Aurora Pearl..." /></label><label><span className="mb-1.5 block text-xs font-bold text-slate-600">Vật liệu & phụ kiện</span><textarea value={designForm.materials} onChange={(event) => setDesignForm((current) => ({ ...current, materials: event.target.value }))} className="min-h-20 w-full rounded-xl border border-slate-200 bg-slate-50 p-3 text-xs outline-none focus:border-violet-400 focus:bg-white focus:ring-4 focus:ring-violet-100" placeholder="Chrome powder, Crystal Mix..." /></label><label className="sm:col-span-2"><span className="mb-1.5 block text-xs font-bold text-slate-600">Kỹ thuật viên đủ kỹ năng</span><input value={designForm.technicians} onChange={(event) => setDesignForm((current) => ({ ...current, technicians: event.target.value }))} className={inputClass} placeholder="Thảo Nguyễn, Hà My..." /></label></div></fieldset><fieldset className="border-t border-slate-100 pt-5"><legend className="mb-3 flex items-center gap-2 text-sm font-black text-slate-800"><Store className="h-4 w-4 text-blue-600" />Phân phối & lưu ý</legend><div className="grid gap-3 sm:grid-cols-3"><label className="flex items-center gap-3 rounded-xl border border-slate-200 bg-slate-50 p-3"><input type="checkbox" checked={designForm.branchQ1} onChange={(event) => setDesignForm((current) => ({ ...current, branchQ1: event.target.checked }))} className="h-4 w-4 accent-violet-600" /><span className="text-xs font-bold text-slate-700">Chi nhánh Quận 1</span></label><label className="flex items-center gap-3 rounded-xl border border-slate-200 bg-slate-50 p-3"><input type="checkbox" checked={designForm.branchQ3} onChange={(event) => setDesignForm((current) => ({ ...current, branchQ3: event.target.checked }))} className="h-4 w-4 accent-violet-600" /><span className="text-xs font-bold text-slate-700">Chi nhánh Quận 3</span></label><label className="flex items-center gap-3 rounded-xl border border-slate-200 bg-slate-50 p-3"><input type="checkbox" checked={designForm.online} onChange={(event) => setDesignForm((current) => ({ ...current, online: event.target.checked }))} className="h-4 w-4 accent-violet-600" /><span className="text-xs font-bold text-slate-700">Hiển thị đặt online</span></label></div><label className="mt-3 block"><span className="mb-1.5 block text-xs font-bold text-slate-600">Lưu ý tư vấn & vận hành</span><textarea value={designForm.notes} onChange={(event) => setDesignForm((current) => ({ ...current, notes: event.target.value }))} className="min-h-20 w-full rounded-xl border border-slate-200 bg-slate-50 p-3 text-xs outline-none focus:border-violet-400 focus:bg-white focus:ring-4 focus:ring-violet-100" /></label></fieldset></> : <><fieldset><legend className="mb-3 flex items-center gap-2 text-sm font-black text-slate-800"><Palette className="h-4 w-4 text-violet-600" />Nhận diện màu sơn</legend><div className="grid gap-3 sm:grid-cols-2"><label><span className="mb-1.5 block text-xs font-bold text-slate-600">Tên màu *</span><input value={colorForm.name} onChange={(event) => setColorForm((current) => ({ ...current, name: event.target.value }))} className={inputClass} placeholder="Bubble Bath" /></label><label><span className="mb-1.5 block text-xs font-bold text-slate-600">Thương hiệu *</span><input value={colorForm.brand} onChange={(event) => setColorForm((current) => ({ ...current, brand: event.target.value }))} className={inputClass} placeholder="OPI" /></label><label><span className="mb-1.5 block text-xs font-bold text-slate-600">Mã màu *</span><input value={colorForm.code} onChange={(event) => setColorForm((current) => ({ ...current, code: event.target.value }))} className={inputClass} placeholder="NL S86" /></label><label><span className="mb-1.5 block text-xs font-bold text-slate-600">Mã HEX tham chiếu</span><div className="flex gap-2"><input type="color" value={colorForm.hex} onChange={(event) => setColorForm((current) => ({ ...current, hex: event.target.value }))} className="h-11 w-14 rounded-xl border border-slate-200 bg-white p-1" /><input value={colorForm.hex} onChange={(event) => setColorForm((current) => ({ ...current, hex: event.target.value }))} className={inputClass} /></div></label><label><span className="mb-1.5 block text-xs font-bold text-slate-600">Hiệu ứng bề mặt</span><BeautifulSelect value={colorForm.finish} onChange={(event) => setColorForm((current) => ({ ...current, finish: event.target.value }))} className={inputClass}>{['Cream · Opaque', 'Sheer · Nude', 'Chrome · Pearl', 'Jelly · Translucent', 'Metallic · Foil', 'Cat Eye · Magnetic'].map((item) => <option key={item}>{item}</option>)}</BeautifulSelect></label><label><span className="mb-1.5 block text-xs font-bold text-slate-600">Bộ sưu tập</span><input value={colorForm.collection} onChange={(event) => setColorForm((current) => ({ ...current, collection: event.target.value }))} className={inputClass} /></label></div></fieldset><fieldset className="border-t border-slate-100 pt-5"><legend className="mb-3 flex items-center gap-2 text-sm font-black text-slate-800"><PackageCheck className="h-4 w-4 text-emerald-600" />Tồn kho & phạm vi</legend><div className="grid gap-3 sm:grid-cols-3"><label><span className="mb-1.5 block text-xs font-bold text-slate-600">Tồn hiện tại</span><input type="number" min="0" value={colorForm.stock} onChange={(event) => setColorForm((current) => ({ ...current, stock: event.target.value }))} className={inputClass} /></label><label><span className="mb-1.5 block text-xs font-bold text-slate-600">Tồn tối thiểu</span><input type="number" min="0" value={colorForm.minimumStock} onChange={(event) => setColorForm((current) => ({ ...current, minimumStock: event.target.value }))} className={inputClass} /></label><label><span className="mb-1.5 block text-xs font-bold text-slate-600">Giá nhập/chai</span><input type="number" min="0" step="10000" value={colorForm.cost} onChange={(event) => setColorForm((current) => ({ ...current, cost: event.target.value }))} className={inputClass} /></label><label><span className="mb-1.5 block text-xs font-bold text-slate-600">Vị trí lưu</span><input value={colorForm.location} onChange={(event) => setColorForm((current) => ({ ...current, location: event.target.value }))} className={inputClass} placeholder="Kệ G-01" /></label><label className="flex items-center gap-3 self-end rounded-xl border border-slate-200 bg-slate-50 p-3"><input type="checkbox" checked={colorForm.branchQ1} onChange={(event) => setColorForm((current) => ({ ...current, branchQ1: event.target.checked }))} className="h-4 w-4 accent-violet-600" /><span className="text-xs font-bold text-slate-700">Chi nhánh Quận 1</span></label><label className="flex items-center gap-3 self-end rounded-xl border border-slate-200 bg-slate-50 p-3"><input type="checkbox" checked={colorForm.branchQ3} onChange={(event) => setColorForm((current) => ({ ...current, branchQ3: event.target.checked }))} className="h-4 w-4 accent-violet-600" /><span className="text-xs font-bold text-slate-700">Chi nhánh Quận 3</span></label></div></fieldset></>}</div><div className="sticky bottom-0 flex justify-end gap-2 border-t border-slate-100 bg-slate-50 px-5 py-4 sm:px-6"><button type="button" onClick={() => setFormMode(null)} className="border border-slate-200 bg-white px-4 text-xs font-bold text-slate-600 shadow-sm">Hủy</button><button type="submit" className="flex items-center gap-2 border border-violet-700 bg-violet-600 px-5 text-xs font-black text-white shadow-lg shadow-violet-200"><Check className="h-4 w-4" />{formMode === 'DESIGN' ? (selectedDesign ? 'Lưu thay đổi' : 'Tạo mẫu Nail') : 'Thêm màu sơn'}</button></div></form></div>}
    </div>
  );
}
